git add -u   # stage all changes / new files in the directory
## git status
git commit -m 'auto_saved'
git push -u origin master


#  Force this version to be the master (overwrite)
#  git push -f origin master
